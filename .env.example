# Generate a secure random SECRET_KEY (required)
SECRET_KEY=change-me-with-a-long-random-string
# Set to false only for local HTTP development (no HTTPS)
COOKIE_SECURE=true

# Port exposed on the host (default: 8080)
PORT=8080
